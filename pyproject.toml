[project]
name = "ai-agents"
version = "0.1.0"
description = "Practice workspace for Python AI agents"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.vercel]
entrypoint = "app:app"
